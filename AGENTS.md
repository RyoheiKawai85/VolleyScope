# VolleyScope Codex開発ガイド

## プロジェクト概要

VolleyScopeは、大学バレーボールの試合映像からボール・選手・プレーを解析し、コーチが大会当日に確認できなかった戦術データを自動生成するシステムである。

最終的には、攻撃位置、攻撃種類、コース、サーブ種類・狙い、セッターのトス配分、得失点傾向などを統計化・可視化する。

## ユーザーの個人目的

VolleyScopeは、単にAIを使用した作品を作るためのプロジェクトではない。

ユーザーは大学バレーボール部でコーチをしていた。大会当日に、自チームへの指示、選手対応、試合準備と並行して対戦相手のデータを記録する必要があり、十分な分析ができなかった。動画を撮影できても、全試合を後から見直して統計化するには大きな時間が必要だった。この実体験を課題設定の出発点とする。

本プロジェクトは、NRI、CTC、ソニーセミコンダクタなどへの就職活動において、次の能力を説明できるガクチカ・ポートフォリオにすることを目的とする。志望先の表記はSCSKではなく、ソニーセミコンダクタである。

- 当事者としての課題発見
- 関係者と利用場面を考慮した要件定義
- 仮説に基づく計画と比較実験
- 結果の批判的解釈
- 失敗を含む改善過程
- 技術選定の根拠
- 実用性と研究上の価値の区別

コードの完成だけでなく、「なぜこの課題を選び、どの仮説を、どの条件で検証し、結果から何を判断したか」を第三者へ説明できる開発履歴を残す。

## 作業開始時に読むファイル

作業前に以下を確認する。

1. `README.md`
2. `docs/DEVELOPMENT_PLAN.md`
3. `docs/EXPERIMENT_LOG.md`
4. `docs/LEARNING_NOTES.md`
5. `docs/NEXT_SESSION.md`

過去の実験結果を無視して、同じ実験や説明を最初から繰り返さない。

## ユーザーへの説明ルール

ユーザーはPythonとGitを学習しながら開発している。

新しいコードを提示するときは、最低限すべてのimportについて以下を説明する。

- importするものは何か
- 何ができるものか
- 今回のコード内でどこに使うか
- 結果として何を調査・実現できるか

新しいコマンドについても、何を対象に何を変更・確認するのか説明する。

既に何度も行った文法確認などは、1コマンドごとに会話を止めず、安全な範囲でまとめて提示する。

プログラミング初心者にも理解できる日本語を使う一方、コード自体は就職活動や研究成果として提示できる品質を保つ。

## 思考方針

提案や実装ではロジカルシンキングとクリティカルシンキングを重視する。

- 実験結果と推測を分ける
- 相関と因果関係を混同しない
- 精度だけでなく処理速度、再現性、実装コストを評価する
- モデルを先に決め打ちしない
- 先行研究の数値を競技やデータが異なるまま直接適用しない
- 実験条件と比較対象を揃える
- 失敗結果も理由とともに記録する
- 実用上の価値と研究上の新規性を分けて考える

各提案・実験・設計判断では、最低限次を明確にする。

1. その作業を行う理由
2. 検証する仮説
3. 比較する代替案
4. 評価条件と指標
5. 結果から言えること
6. 結果から言えないこと
7. 次の判断または改善への接続

都合のよい結果だけを採用せず、失敗、反証、制約も記録する。先行研究との比較では、競技、データ、分割方法、入力条件、評価指標の違いを明示し、数値だけを直接比較しない。

## データ分割

現在の104枚の正例を含む`evaluation_001`は評価用データであり、学習へ使用しない。

学習用、検証用、最終テスト用は、可能な限り試合・体育館単位で分割する。隣接フレームをランダムにtrainとtestへ分けてデータリークを起こさない。

## モデル方針

YOLOだけに固定しない。

- 選手・ネット・コート：YOLO系モデル
- ボール：追加学習したYOLOとTrackNet系モデルを比較
- 軌道補完：基本検出性能を確立した後に評価
- 戦術分析：検出・追跡結果を時系列データ化した後に実装

最初の目標は、画面上で視認可能な小さいボールを高いRecallで検出することである。完全遮蔽は後段の追跡・軌道補完として扱う。

## Git運用

動画、抽出フレーム、モデル重み、大量の出力結果、仮想環境はGitHubへ登録しない。

コミット前に以下を行う。

- Pythonの文法確認
- `git diff --check`
- `git status`
- ステージング対象の確認

大量データを誤登録しないため、原則として`git add .`ではなく対象ファイルを明示する。

ユーザーの既存変更を勝手に削除しない。履歴を書き換える破壊的なGit操作は行わない。

文法確認、`git diff --check`、`git status`など、既に反復した安全な確認はまとめて実行してよい。一方、破壊的操作、ライブラリのインストール、Git競合の解消、重要な設計判断ではユーザーへ確認する。

## 開発環境

OSはWindows、ターミナルはPowerShellを使用する。

YOLO環境とTrackNet環境は分離する。既存の`.venv`へTrackNetの古い依存関係を直接追加しない。

ローカルパスをコードへ固定せず、`pathlib.Path`を利用してプロジェクトルートからパスを構築する。

## ドキュメント

一日の最後に以下を更新する。

- 実験条件・結果・考察：`docs/EXPERIMENT_LOG.md`
- 新しく学習したPython・Git知識：`docs/LEARNING_NOTES.md`
- 方針変更：`docs/DEVELOPMENT_PLAN.md`
- 次回の開始地点：`docs/NEXT_SESSION.md`

その後、文法確認、コミット、GitHubへのプッシュまで行う。
